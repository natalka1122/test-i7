{
    "variables": {
        "project_id": null,
        "source_image": "debian-10-buster-v20191014",
        "machine_type": "f1-small",
        "image_name": "test-i7-{{timestamp}}",
        "zone": "europe-west1-b",
        "username": null
},
    "builders": [
        {
            "type": "googlecompute",
            "project_id": "{{user `project_id`}}",
            "image_name": "{{user `image_name`}}",
            "source_image": "{{user `source_image`}}",
            "zone": "{{user `zone`}}",
            "ssh_username": "{{user `username`}}",
            "machine_type": "{{user `machine_type`}}",
            "image_description": "Image based on Debian 10. PowerDNS API + Nginx + MariaDB"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "script": "scripts/deploy.sh",
            "pause_before": "10s"
        }
    ]
}